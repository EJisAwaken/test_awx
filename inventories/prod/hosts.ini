[all]
host1 ansible_host=10.0.36.55 ansible_user=vboxuser

[all:vars]
# Recommandé: utiliser des clés SSH plutôt qu'un mot de passe
# ansible_ssh_private_key_file=~/.ssh/id_rsa
# Si vous devez utiliser un mot de passe SSH, préférez Ansible Vault (group_vars) et --ask-vault-pass
# ansible_ssh_pass=...

# Pour sudo (become), lancez le playbook avec --ask-become-pass
# ou définissez ansible_become_password via Ansible Vault.

